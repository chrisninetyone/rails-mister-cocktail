<div class="container">
  <h1>Cocktails</h1>
  <div>
    <% @cocktails.each do |cocktail| %>
        <p>
        <%= link_to cocktail.name, cocktail_path(cocktail) %>
        </p>
    <% end %>
  </div>
  <%= link_to "Create Cocktail", new_cocktail_path, class: "btn btn-dark" %>
</div>

