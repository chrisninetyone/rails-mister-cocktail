<div class="container">
  <h1><%= @cocktail.name %></h1>
  <p>Mmm</p>
  <%= simple_form_for [@cocktail, @dose] do |f|%>
    <%= f.input :description %>
    <%= f.association :ingredient  %>
    <%= f.submit %>
  <% end %>

  <% @cocktail.doses.each do |dose| %>
    <%= dose.description %>
    <%= dose.ingredient.name %>
    <%= link_to "Delete this Dose", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %>
  <% end %>


  <%= link_to "Back to List", cocktails_path %>

</div>

<!--
link to create new doses
embed the form or route to another page (define @dose)
Show page should display the name and the doses (and all its elements--description, ingredients)
 -->
